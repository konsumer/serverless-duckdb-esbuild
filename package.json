{
  "name": "serverless-duckdb-esbuild",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sls offline",
    "deploy": "sls deploy"
  },
  "devDependencies": {
    "serverless": "^3.34.0",
    "serverless-esbuild": "^1.46.0",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "duckdb": "^0.8.1"
  }
}
